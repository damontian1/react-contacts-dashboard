import React from "react";
import ReactDOM from "react-dom";
import App from "./components/App";

// console.log(App)
// 1. create the view structure
// 2. create flux's folder structure
// 3. view features from api:
  // a. integrate and read from firebase database
  // a. add a contact
  // b. delete a contact
  // c. update a contact

ReactDOM.render(<App/>, document.getElementById("root"))